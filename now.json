{
  "version": 2,
  "routes": [{"src": "/(.*)", "dest": "/build/$1"}],
  "builds": [
    {
      "src": "package.json",
      "use": "@now/static-build",
      "config": { "distDir": "build" }
    }]
}